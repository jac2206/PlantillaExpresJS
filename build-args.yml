variables:
  environmentVariables: |
    --build-arg PORT="$(PORT)"
    --build-arg NODE_ENV="$(NODE_ENV)"
    --build-arg AXIOS_HTTP_TIMEOUT="$(AXIOS_HTTP_TIMEOUT)"
    --build-arg CORS_ORIGINS="$(CORS_ORIGINS)"
    --build-arg NEW_RELIC_KEY="$(NEW_RELIC_KEY)"
    --build-arg HSTS_MAX_AGE="$(HSTS_MAX_AGE)"
    --build-arg APIGATEWAY_BASE_URL="$(APIGATEWAY_BASE_URL)"
    --build-arg APIGATEWAY_URL_BEARER_TOKEN="$(APIGATEWAY_URL_BEARER_TOKEN)"
    --build-arg APIGATEWAY_CA_CHANNEL="$(APIGATEWAY_CA_CHANNEL)"
    --build-arg APIGATEWAY_PASSPHRASE="$(APIGATEWAY_PASSPHRASE)"
    --build-arg APIGATEWAY_PFX_PATH="$(APIGATEWAY_PFX_PATH)"
    --build-arg APIGATEWAY_CLIENT_ID="$(APIGATEWAY_CLIENT_ID)"
    --build-arg APIGATEWAY_CLIENT_SECRET="$(APIGATEWAY_CLIENT_SECRET)"
    --build-arg POS_SHORT_BALANCE="$(POS_SHORT_BALANCE)"
    --build-arg SPLUNK_BASE_URL="$(SPLUNK_BASE_URL)"
    --build-arg SPLUNK_AUDIT_PATH="$(SPLUNK_AUDIT_PATH)"
    --build-arg SPLUNK_TOKEN="$(SPLUNK_TOKEN)"
    --build-arg SPLUNK_INDEX="$(SPLUNK_INDEX)"
    --build-arg SPLUNK_SOURCE="$(SPLUNK_SOURCE)"
    --build-arg AWS_REGION="$(AWS_REGION)"
    --build-arg POSTGRES_DIALECT="$(POSTGRES_DIALECT)"
    --build-arg POSTGRES_HOST="$(POSTGRES_HOST)"
    --build-arg POSTGRES_PORT="$(POSTGRES_PORT)"
    --build-arg POSTGRES_USER="$(POSTGRES_USER)"
    --build-arg POSTGRES_PASS="$(POSTGRES_PASS)"