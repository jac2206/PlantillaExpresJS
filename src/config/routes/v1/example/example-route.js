// import express from 'express';
// import { getLocation, saveLocation, getLocations } from '../../../../web-api/controllers/location-controller';

// export const locationRoutes = () => {
//   const router = express.Router();
//   router.get('/', getLocations);
//   router.get('/:locationCode', getLocation);
//   // router.get('/partner/:partnerCode', getLocationsXPartnerCode);
//   router.post('/', saveLocation);
//   return router;
// };
